These templates override the default nPress templates from libs/npress/FrontModule/templates/*, etc.

Only one naming convention is allowed here:
	Module.Presenter.default.latte

Also layout files can be overriden here, all below is possible:
	Module.Presenter.@layout.latte
	Module.@layout.latte
	@layout.latte